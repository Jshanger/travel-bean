import type { ClickedWordHistoryItem } from '../types';
export function ClickedHistory({items,onOpen,onClear}:{items:ClickedWordHistoryItem[];onOpen:(item:ClickedWordHistoryItem)=>void;onClear:()=>void}){if(!items.length)return null;return <section className="history-section"><div className="history-head"><div><span className="step pale">3</span><div><h2>Recently explored</h2><p>Your latest clicked words</p></div></div><button className="text-button" onClick={onClear}>Clear history</button></div><div className="history-strip">{items.slice(0,8).map(item=><button className="history-card" key={item.id} onClick={()=>onOpen(item)}><span>{item.chinese}</span><div><b>{item.pinyin}</b><small>{item.meaning}</small></div><i>→</i></button>)}</div></section>}
